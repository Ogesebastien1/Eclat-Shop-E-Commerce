{# This is the email template for password reset #}

<h1>Hi!</h1>

<p>
    {# Instruction to reset password #}
    To reset your password, please visit the following link
</p>

{# Password reset link #}
<a href="http://localhost:3010/modify-password?token={{ resetToken.token }}">
    http://localhost:3010/modify-password?token={{ resetToken.token }}
</a>

<p>
    {# Information about link expiration #}
    This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
</p>

<p>Cheers!</p>